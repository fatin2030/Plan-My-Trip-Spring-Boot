spring.application.name=PlanMyTrip
spring.datasource.url=jdbc:postgresql://localhost:5432/plan_my_trip
spring.datasource.username=postgres
spring.datasource.password=greentea2000
spring.datasource.driver-class-name=org.postgresql.Driver

# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Show full stacktrace in JSON error responses
server.error.include-message=always
#server.error.include-binding-errors=always
#server.error.include-stacktrace=always

# Optional: Enable debug logging for your classes
logging.level.com.example=DEBUG
